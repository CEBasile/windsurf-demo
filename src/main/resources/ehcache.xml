<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xsi:schemaLocation="
            http://www.ehcache.org/v3 
            http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <!-- Cache configuration for tickets -->
    <cache alias="tickets">
        <key-type>java.lang.Long</key-type>
        <value-type>com.ticketapp.model.Ticket</value-type>
        <expiry>
            <!-- Cache entries expire after 10 minutes of creation -->
            <ttl unit="minutes">10</ttl>
        </expiry>
        <resources>
            <!-- Keep up to 100 tickets in memory -->
            <heap unit="entries">100</heap>
        </resources>
    </cache>

    <!-- Cache configuration for user tickets (by createdBy) -->
    <cache alias="userTickets">
        <key-type>java.lang.String</key-type>
        <value-type>java.util.List</value-type>
        <expiry>
            <!-- Cache entries expire after 5 minutes of creation -->
            <ttl unit="minutes">5</ttl>
        </expiry>
        <resources>
            <!-- Keep up to 50 user ticket lists in memory -->
            <heap unit="entries">50</heap>
        </resources>
    </cache>

</config>
